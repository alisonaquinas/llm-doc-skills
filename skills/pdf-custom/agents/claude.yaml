interface:
  display_name: "PDF Processing Skill - Custom"
  short_description: "Merge, split, extract, create, and OCR PDF files"
  icon_small: "./assets/icon.svg"
  icon_large: "./assets/icon.svg"
  default_prompt: "Use $pdf-custom to process PDFs: merge, split, extract text/tables, fill forms, or create new PDFs."
